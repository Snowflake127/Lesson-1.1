#Задача "Счётчик вызовов"

calls = 0
def string_info (string):
    string_info_tuple=(len(string), string.upper(),string.lower())
    print(string_info_tuple)
    def count_calls ():
        global calls
        calls += 1
        #print(calls)
    count_calls()


def is_contains (string, list_strings):
    for i in range(0, len (list_strings)):
        list_strings[i] = list_strings[i].lower()
    print(list_strings)
    print(string.lower() in list_strings)
    def count_calls ():
        global calls
        calls+=1
        #print(calls)
    count_calls ()
    print (calls)

string_info('We are the champions')
string_info ('Capybara')
string_info ('Armageddon')
is_contains ('Urban',['ban', 'BaNaN', 'urBAN'])
is_contains('cycle', ['recycling', 'cyclic'])
